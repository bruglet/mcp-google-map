# Upstream deviations

Base: CabLate `cablate/mcp-google-map` commit `be966cc`.

This file is updated in the same change as each deviation to an upstream-owned file. New policy and planner behavior is kept in additive modules where possible. The table below is intentionally explicit so future upstream merges can be reviewed by responsibility rather than by an opaque fork diff.

| Upstream-owned file                                                                                                                         | Original responsibility                                                  | Fork behavior changed                                                                                                                                        | Delegated/new modules                                                  | Compatibility and merge surface                                                                         | Regression protection                               |
| ------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| `package.json`, `package-lock.json`, `server.json`                                                                                          | npm/MCP package identity and metadata                                    | Fork identity is `bruglet`; package is private for local builds; `server.json` advertises no npm/OCI package; container-only publication metadata            | `.github/workflows/ci.yml`, `README.md`                                | Package consumers must use the container or local source; metadata-only conflict                        | Build and workflow inspection                       |
| `CONTRIBUTING.md`, `SECURITY_ASSESSMENT.md`, `skills/project-docs/SKILL.md`, `skills/project-docs/references/*`                             | Upstream contribution, security, and developer-orientation documentation | Fork URLs, Node 24/runtime boundary, server-side credentials, Cloudflare origin validation, and historical-reference warnings are documented                 | `docs/ARCHITECTURE.md`, `docs/DEPLOYMENT.md`                           | Documentation-only changes; historical upstream reference files remain clearly subordinate to fork docs | Documentation review and credential-boundary search |
| `Dockerfile`                                                                                                                                | Node 20 single-stage image                                               | Node 24 multi-stage, production deps only, UID 10001, labels, healthcheck, read-only-compatible runtime                                                      | `docs/DEPLOYMENT.md`                                                   | Image build file is a deliberate replacement                                                            | Container workflow smoke test                       |
| `.github/workflows/ci.yml`, deleted `release.yml`                                                                                           | Basic CI and npm/Registry release                                        | Checks-first CI; GHCR-only multiarch publication; explicit fork OCI labels; SHA tags, SBOM, provenance, anonymous public-pull verification, no npm token     | `.github/dependabot.yml`                                               | Workflow replacement; do not reintroduce npm or MCP Registry publishing                                 | Required job dependency and PR no-push condition    |
| `src/config.ts`, `src/cli.ts`                                                                                                               | Tool registration and CLI dispatch                                       | New cost/transit tools registered; weather/AQ/static/timezone/rank tools unreachable; allowlists fail closed; new route arguments forwarded                  | `src/tools/maps/*`, `src/services/*`                                   | Registration and switch cases are narrow merge points                                                   | `urlAndConfig.unit.test.ts`, build                  |
| `src/core/BaseMcpServer.ts`                                                                                                                 | Streamable HTTP sessions and health                                      | `/healthz` reports safe readiness/config booleans; `/mcp` validates Cloudflare Access; sessions close during shutdown                                        | `src/utils/cloudflareAuth.ts`                                          | Transport behavior retained; auth is a production boundary                                              | Live/manual auth tests and container health test    |
| `src/services/NewPlacesService.ts`                                                                                                          | Places New client calls                                                  | Semantic masks, one details request, no Legacy review/photo fan-out, accounting                                                                              | `src/services/costPolicy.ts`, `requestAccounting.ts`, `usageLedger.ts` | Client adapter remains recognizable; request construction is the main conflict surface                  | `mapsRequest.unit.test.ts`, cost policy tests       |
| `src/services/PlacesSearcher.ts`                                                                                                            | Search, details, routes, composite helpers                               | Minimal defaults, local URLs, explicit enrichment and transit, no hardcoded driving comparison                                                               | `mapsUrlService.ts`, `TransitItineraryService.ts`                      | Public method signatures expanded with optional policy fields                                           | request and planner unit tests                      |
| `src/services/RoutesService.ts`                                                                                                             | Directions and matrices                                                  | Dynamic masks, traffic opt-in, transit validation, fractional durations, element failures preserved, accounting                                              | `costPolicy.ts`, `requestAccounting.ts`                                | Route request body and masks are deliberate conflict points                                             | `mapsRequest.unit.test.ts`                          |
| `src/services/toolclass.ts`                                                                                                                 | Legacy Google helper operations                                          | Along-route request uses internal geometry and minimal Places mask                                                                                           | `costPolicy.ts`, URL service                                           | Narrow field-mask/output change                                                                         | build and route request tests                       |
| `src/utils/apiKeyManager.ts`                                                                                                                | Header/session/default key precedence                                    | Compatibility facade reads environment only; no Google header or bearer interpretation                                                                       | `requestContext.ts`, `cloudflareAuth.ts`                               | Old imports remain source-compatible only for read access                                               | credential-boundary review and live no-header path  |
| `src/tools/maps/{placeDetails,directions,distanceMatrix,searchPlaces,searchNearby,exploreArea,planRoute,comparePlaces,searchAlongRoute}.ts` | MCP schemas/actions                                                      | Semantic enrichment, safe route options, minimal defaults, local URLs                                                                                        | Cost policy and URL service                                            | Parameter schemas intentionally changed; old `maxPhotos` is removed                                     | request regression suite                            |
| `skills/google-maps/references/tools-api.md`, `local-seo.md`                                                                                | Upstream tool and local-SEO reference documentation                      | Current tool reference documents the fork contract; local-SEO material is explicitly marked historical because disabled photo/rank tools are not v1 features | `skills/google-maps/SKILL.md`, `docs/COST_POLICY.md`                   | Documentation-only changes; prevents agents from selecting disabled or high-cost inherited parameters   | Documentation review and stale-contract scan        |
| `docs/research-ai-place-selection.md`                                                                                                       | Upstream feasibility research                                            | Explicitly marked historical so its old tool examples and cost assumptions cannot override the current fork contract                                         | `docs/USE_CASES.md`, `docs/COST_POLICY.md`                             | Documentation-only change                                                                               | Documentation review                                |

## Intentionally retained but unreachable source

The upstream weather, air-quality, timezone, static-map, and local-rank source files remain where removal would make future upstream synchronization needlessly noisy. They are not imported by `config.ts`, not present in the active tool allowlist, and not listed in the active CLI execution table. Their stale documentation is not copied into the runtime image.

## Upstream synchronization checklist

1. Fetch `upstream/main` and inspect upstream changes before merging.
2. Re-run `npm ci`, `npm run test:unit`, `npm run build`, `npm run lint`, and `npm run format:check`.
3. Re-check Places/Routes official SKU and field documentation.
4. Re-run the container, auth, ledger, Quadlet, and loopback boundary checks.
5. Update this file in the same commit for every new upstream-owned-file deviation.
